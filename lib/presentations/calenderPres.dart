import 'package:flutter/material.dart';

class CalenderPres extends StatelessWidget {
  const CalenderPres({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Calendar')),
    );
  }
}
